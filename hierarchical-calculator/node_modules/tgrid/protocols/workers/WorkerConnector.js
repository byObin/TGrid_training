"use strict";
var __extends = (this && this.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.WorkerConnector = void 0;
//================================================================ 
/** @module tgrid.protocols.workers */
//================================================================
var Communicator_1 = require("../../components/Communicator");
var IConnector_1 = require("../internal/IConnector");
var once_1 = require("../internal/once");
var DomainError_1 = require("tstl/exception/DomainError");
var node_1 = require("tstl/utility/node");
var global_1 = require("tstl/thread/global");
/**
 * Worker Connector.
 *
 * The `WorkerConnector` is a communicator class, who can create an `Worker` instance and
 * communicate with it using RFC (Remote Function Call), considering the `Worker` as a
 * remote system ({@link WorkerServer}).
 *
 * You can create an `Worker` instance with {@link compile}() or {@link connect}() method.
 * Anyway, after creation of the `Worker` instance, the `Worker` program must open a server
 * using the {@link WorkerServer.open}() method.
 *
 * Note that, after your business, don't forget terminating the worker using {@link close}()
 * or {@link WorkerServer.close}(). If you don't terminate it, then vulnerable memory and
 * communication channel would not be destroyed and it may cause the memory leak.
 *
 * @type Provider Type of features provided for remote system.
 * @author Jeongho Nam - https://github.com/samchon
 */
var WorkerConnector = /** @class */ (function (_super) {
    __extends(WorkerConnector, _super);
    /* ----------------------------------------------------------------
        CONSTRUCTOR
    ---------------------------------------------------------------- */
    /**
     * Initializer Constructor.
     *
     * @param provider An object providing features for remote system.
     */
    function WorkerConnector(provider) {
        var _this = _super.call(this, provider) || this;
        // ASSIGN MEMBERS
        _this.state_ = -1 /* NONE */;
        return _this;
    }
    /**
     * Compile server and connect to there.
     *
     * The {@link compile} method tries compile JS source code, creates `Worker` instance
     * with that code connects to the `Worker`. To complete the compilation and connection,
     * the `Worker` program must open that server using the {@link WorkerServer.open}()
     * method.
     *
     * Note that, after your business has been completed, you've to close the `Worker` using
     * {@link close}() or {@link WorkerServer.close}(). If you don't close that, vulnerable
     * memory usage and communication channel would not be destroyed and it may cause the
     * memory leak.
     *
     * @param content JS Source code to compile.
     * @param headers Headers containing initialization data like activation.
     * @param timeout Milliseconds to wait the worker program to open itself. If omitted, the waiting would be forever.
     */
    WorkerConnector.prototype.compile = function (content, headers, timeout) {
        return __awaiter(this, void 0, void 0, function () {
            var path, error, exp_1;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        //----
                        // PRELIMINIARIES
                        //----
                        // TEST CONDITION
                        this._Test_connection("compile");
                        return [4 /*yield*/, Compiler.compile(content)];
                    case 1:
                        path = _a.sent();
                        error = null;
                        _a.label = 2;
                    case 2:
                        _a.trys.push([2, 4, , 5]);
                        return [4 /*yield*/, this._Connect("compile", path, headers, timeout)];
                    case 3:
                        _a.sent();
                        return [3 /*break*/, 5];
                    case 4:
                        exp_1 = _a.sent();
                        error = exp_1;
                        return [3 /*break*/, 5];
                    case 5: 
                    // REMOVE THE TEMPORARY FILE
                    return [4 /*yield*/, Compiler.remove(path)];
                    case 6:
                        // REMOVE THE TEMPORARY FILE
                        _a.sent();
                        // LAZY THROWING
                        if (error !== null)
                            throw error;
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * Connect to server.
     *
     * The {@link connect}() method tries to create an `Worker` instance and connect to the
     * `Worker`. To complete the connection, the `Worker` program must open that server using
     * the {@link WorkerServer.open}() method.
     *
     * Note that, after your business has been completed, you've to close the `Worker` using
     * {@link close}() or {@link WorkerServer.close}(). If you don't close that, vulnerable
     * memory usage and communication channel would not be destroyed and it may cause the
     * memory leak.
     *
     * @param jsFile JS File to be {@link WorkerServer}.
     * @param args Headers containing initialization data like activation.
     * @param timeout Milliseconds to wait the worker program to open itself. If omitted, the waiting would be forever.
     */
    WorkerConnector.prototype.connect = function (jsFile, headers, timeout) {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        // TEST CONDITION
                        this._Test_connection("connect");
                        // DO CONNECT
                        return [4 /*yield*/, this._Connect("connect", jsFile, headers, timeout)];
                    case 1:
                        // DO CONNECT
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    /**
     * @hidden
     */
    WorkerConnector.prototype._Test_connection = function (method) {
        if (this.worker_ && this.state !== 3 /* CLOSED */) {
            if (this.state_ === 0 /* CONNECTING */)
                throw new DomainError_1.DomainError("Error on WorkerConnector." + method + "(): on connecting.");
            else if (this.state_ === 1 /* OPEN */)
                throw new DomainError_1.DomainError("Error on WorkerConnector." + method + "(): already connected.");
            else
                throw new DomainError_1.DomainError("Error on WorkerConnector." + method + "(): closing.");
        }
    };
    /**
     * @hidden
     */
    WorkerConnector.prototype._Connect = function (method, jsFile, headers, timeout) {
        return __awaiter(this, void 0, void 0, function () {
            var at, exp_2;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        at = (timeout !== undefined)
                            ? new Date(Date.now() + timeout)
                            : undefined;
                        // SET CURRENT STATE
                        this.state_ = 0 /* CONNECTING */;
                        _a.label = 1;
                    case 1:
                        _a.trys.push([1, 4, , 5]);
                        // EXECUTE THE WORKER
                        this.worker_ = Compiler.execute(jsFile);
                        return [4 /*yield*/, this._Handshake(method, timeout, at)];
                    case 2:
                        // WAIT THE WORKER TO BE READY
                        if ((_a.sent()) !== 0 /* CONNECTING */)
                            throw new DomainError_1.DomainError("Error on WorkerConnector." + method + "(): target worker may not be opened by TGrid. It's not following the TGrid's own handshake rule when connecting.");
                        // SEND HEADERS
                        this.worker_.postMessage(JSON.stringify(headers));
                        return [4 /*yield*/, this._Handshake(method, timeout, at)];
                    case 3:
                        // WAIT COMPLETION
                        if ((_a.sent()) !== 1 /* OPEN */)
                            throw new DomainError_1.DomainError("Error on WorkerConnector." + method + "(): target worker may not be opened by TGrid. It's not following the TGrid's own handshake rule when connected.");
                        // SUCCESS
                        this.state_ = 1 /* OPEN */;
                        this.worker_.onmessage = this._Handle_message.bind(this);
                        return [3 /*break*/, 5];
                    case 4:
                        exp_2 = _a.sent();
                        try {
                            if (this.worker_)
                                this.worker_.terminate();
                        }
                        catch (_b) { }
                        this.state_ = -1 /* NONE */;
                        throw exp_2;
                    case 5: return [2 /*return*/];
                }
            });
        });
    };
    WorkerConnector.prototype._Handshake = function (method, timeout, until) {
        var _this = this;
        return new Promise(function (resolve, reject) {
            var completed = false;
            var expired = false;
            if (until !== undefined)
                global_1.sleep_until(until).then(function () {
                    if (completed === false) {
                        reject(new DomainError_1.DomainError("Error on WorkerConnector." + method + "(): target worker is not sending handshake data over " + timeout + " milliseconds."));
                        expired = true;
                    }
                });
            _this.worker_.onmessage = once_1.once(function (evt) {
                if (expired === false) {
                    completed = true;
                    resolve(evt.data);
                }
            });
        });
    };
    /**
     * @inheritDoc
     */
    WorkerConnector.prototype.close = function () {
        return __awaiter(this, void 0, void 0, function () {
            var error, ret;
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0:
                        error = this.inspectReady("WorkerConnector.close");
                        if (error)
                            throw error;
                        ret = this.join();
                        // REQUEST CLOSE TO SERVER
                        this.state_ = 2 /* CLOSING */;
                        this.worker_.postMessage(2 /* CLOSING */);
                        // LAZY RETURN
                        return [4 /*yield*/, ret];
                    case 1:
                        // LAZY RETURN
                        _a.sent();
                        return [2 /*return*/];
                }
            });
        });
    };
    Object.defineProperty(WorkerConnector.prototype, "state", {
        /* ----------------------------------------------------------------
            ACCESSORS
        ---------------------------------------------------------------- */
        /**
         * @inheritDoc
         */
        get: function () {
            return this.state_;
        },
        enumerable: false,
        configurable: true
    });
    /* ----------------------------------------------------------------
        COMMUNICATOR
    ---------------------------------------------------------------- */
    /**
     * @hidden
     */
    WorkerConnector.prototype.sendData = function (invoke) {
        this.worker_.postMessage(JSON.stringify(invoke));
    };
    /**
     * @hidden
     */
    WorkerConnector.prototype.inspectReady = function (method) {
        return IConnector_1.IConnector.inspect(this.state_, "WorkerConnector." + method + "()");
    };
    /**
     * @hidden
     */
    WorkerConnector.prototype._Handle_message = function (evt) {
        if (evt.data === 2 /* CLOSING */)
            this._Handle_close();
        else
            this.replyData(JSON.parse(evt.data));
    };
    /**
     * @hidden
     */
    WorkerConnector.prototype._Handle_close = function () {
        return __awaiter(this, void 0, void 0, function () {
            return __generator(this, function (_a) {
                switch (_a.label) {
                    case 0: 
                    // STATE & PROMISE RETURN
                    return [4 /*yield*/, this.destructor()];
                    case 1:
                        // STATE & PROMISE RETURN
                        _a.sent();
                        this.state_ = 3 /* CLOSED */;
                        return [2 /*return*/];
                }
            });
        });
    };
    return WorkerConnector;
}(Communicator_1.Communicator));
exports.WorkerConnector = WorkerConnector;
(function (WorkerConnector) {
})(WorkerConnector = exports.WorkerConnector || (exports.WorkerConnector = {}));
exports.WorkerConnector = WorkerConnector;
//----
// CAPSULIZATION
//----
/**
 * @hidden
 */
var Compiler = node_1.is_node()
    ? require("./internal/node-worker")
    : require("./internal/web-worker");
//# sourceMappingURL=WorkerConnector.js.map